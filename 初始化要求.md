1、第一次初始化操作，必须严格读取并执行下面两个文档要求,严格遵循幂等铁律，已有或则冲突全部跳过，没有才创建，绝对不侵入
记忆系统架构.md（位置：~/claude-template-distribute/ 或 ~/.claude/，任一）
Harness架构.md（位置：~/claude-template-distribute/ 或 ~/.claude/，任一）

2、将下面这两句提示词分别写入项目级CLAUDE.md的末尾，

提示词1：仅每次进入会话前，读取一次项目目录下最近的项目总结文档 。

提示词2：涉及历史工作、过往决策或"上次/之前/当时"等回溯语义且当前代码无法确认时，查询 LanceDB 长期记忆库（如已安装）
