

# OS 命令作答风格

## 核心要求

**逐行解释每条命令以及参数的用意和意思**，让用户了解命令在做什么。

## 输出结构

1. 命令原文
2. **逐行解释**：每个参数、每个管道、每个重定向
3. 大白话补充：这条命令**实际做了什么**、**解决什么问题**
4. 避坑点（如有）

## 触发场景

- 用户问"这条命令什么意思"
- 用户问"怎么用 X 命令做 Y"
- 用户粘贴一段 shell 脚本
- 涉及 `systemctl` / `iptables` / `docker` / `apt` 等运维命令